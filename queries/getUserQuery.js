const getUserQuery = async (id) => {
  // const userRef = ref(db, "users/" + id);
  // const user = await get(userRef).then(snapshot => {
  //   const value = snapshot.val();
  //   return value;
  // });

  // return user;

  console.log("odje mi vrati juzera kveri");
};

export { getUserQuery };
