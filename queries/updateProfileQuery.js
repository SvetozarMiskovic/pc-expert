const updateProfileQuery = async (data, id) => {
  // const userRef = ref(db, "users/" + id);
  // const isUpdated = await update(userRef, data)
  //   .then(() => true)
  //   .catch(() => false);

  // // console.log(isUpdated);
  // return isUpdated;
  console.log("odje updejtuj profil kveri");
};

export { updateProfileQuery };
