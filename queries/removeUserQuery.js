const removeUserQuery = async (id) => {
  // const usersRef = ref(db, "users/" + `${id}`);
  // const removed = await remove(usersRef).then(() => {
  //   console.log(`Removed user ${id}`);
  // });

  // return removed;
  console.log("odje obrisi juzera kveri");
};

export { removeUserQuery };
